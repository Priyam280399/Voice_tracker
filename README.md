# Voice Task Tracker

This archive contains a backend (Express + MongoDB) and frontend (React + Vite) with AssemblyAI-based STT integration.

See backend/.env.example for required environment variables.
"# Voice_tracker" 
